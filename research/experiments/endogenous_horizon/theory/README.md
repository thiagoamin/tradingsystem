# Endogenous Horizon -- Theory

Theory documents for the endogenous-horizon allocation work.

| File | Role |
|---|---|
| `core_theory/core_theory.tex` / `.pdf` | Core theoretical framework for endogenously selecting trading horizons across signal sleeves. |

LaTeX build artifacts (`.aux`, `.fdb_latexmk`, etc.) live alongside source so
the PDFs are reproducible. Regenerate with `latexmk -pdf core_theory.tex` if
needed.
